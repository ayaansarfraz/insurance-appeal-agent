# Copy to .env.local and fill in. .env.local is gitignored and does NOT carry
# across git worktrees, so copy it into each worktree you run.

# Mireye: sign up at mireye.com/account with code BUILD for a free Build tier.
MIREYE_API_KEY=

# Anthropic: console.anthropic.com
ANTHROPIC_API_KEY=
